[project]
name = "aitools"
version = "0.1.0"
description = "学习nanobot所实践的demo"
authors = [
    {name = "therzyx"}
]
requires-python = ">=3.13"
dependencies = [
    "rich (>=14.3.3,<15.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
